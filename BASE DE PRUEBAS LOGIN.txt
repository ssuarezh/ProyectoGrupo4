//Nombre de la base: pruebalogin

CREATE TABLE usuario (
   id tinyint(3) unsigned not null auto_increment primary key,
   nombre varchar(50) not null,
   contrasena varchar(50) not null
)ENGINE=innoDB;

INSERT INTO usuario(nombre, contrasena)
VALUES('admin', '1234');